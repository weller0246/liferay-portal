task runGradleTest

runGradleTest {
	dependsOn ":sampleThemeFavicon:buildClientExtension"

	doLast {
		def sampleProject = project(":sampleThemeFavicon")

		File sampleConfigFile = new File(sampleProject.buildDir, "sample.client-extension-config.json")

		assert sampleConfigFile.exists()
	}
}